# 视觉策划台使用说明

## 问题说明

如果您在终端直接粘贴文案时遇到 "command not found" 错误，这是因为文案被当作 shell 命令执行了。

## 解决方案

### 方式1: 从文件读取（推荐）⭐

1. **将文案保存为文本文件**（UTF-8 编码）
   ```bash
   # 创建一个文本文件，将文案内容保存进去
   nano input_text.txt
   # 或使用其他编辑器，保存为 UTF-8 编码
   ```

2. **运行程序并选择方式1**
   ```bash
   python3 tools/visual_studio.py
   # 选择 1（从文件读取）
   # 输入文件路径（例如：input_text.txt 或 /完整路径/to/input_text.txt）
   ```

### 方式2: 交互式输入

1. **先运行程序**
   ```bash
   python3 tools/visual_studio.py
   ```

2. **等待程序提示输入**
   - 程序会显示 "请粘贴您的文案内容："
   - **此时再粘贴文案**

3. **输入完成后**
   - 在新的一行输入 `END` 并按回车结束

## 常见问题

### Q: 为什么会出现 "command not found"？

A: 这是因为在 shell 中直接粘贴了文案，zsh 把这些文本当作命令执行了。**必须先运行程序，然后在程序的交互式提示中粘贴。**

### Q: 如何避免编码问题？

A: 
1. 使用方式1（从文件读取）
2. 确保文本文件是 UTF-8 编码
3. 或者在运行前设置环境变量：
   ```bash
   export LANG=en_US.UTF-8
   export PYTHONIOENCODING=utf-8
   ```

### Q: 支持哪些文件编码？

A: 程序会自动尝试以下编码：
- UTF-8（优先）
- GBK
- GB2312
- UTF-16

## 使用示例

### 示例1: 从文件读取

```bash
# 1. 创建文案文件
cat > my_text.txt << 'EOF'
标题：职场新人必看！这5个坑千万别踩

正文：
刚入职场的你，是不是也遇到过这些情况？
...
EOF

# 2. 运行程序
python3 tools/visual_studio.py
# 选择 1
# 输入文件路径: my_text.txt
```

### 示例2: 交互式输入

```bash
# 1. 运行程序
python3 tools/visual_studio.py

# 2. 选择 2（交互式输入）

# 3. 等待提示后粘贴文案
# 请粘贴您的文案内容：

# 4. 粘贴文案（使用 Ctrl+V 或 Cmd+V）

# 5. 输入 END 结束
END
```

## 输出

程序会：
1. 在终端显示视觉策划方案
2. 自动保存为 Markdown 文件：`output/prompts/visual_plan_[timestamp].md`

您可以直接打开 Markdown 文件，复制提示词到 Midjourney 或 Imagen 中使用。
